function 

//callbacks
//son la declaracion de una funcion que pasa como parametro a otra funcion para ser ejecutada en el futuro.

const array = [2,3,4,5]
array.map((number, index) => {
    console.log(number)
})